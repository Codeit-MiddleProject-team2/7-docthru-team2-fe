import TranslationDetail from "@/components/translation/TranslationDetail";

export default function TranslationDetailPage() {
  // 추후에 translationId로 데이터 fetch 가능 (현재는 mock)
  return <TranslationDetail />;
}
